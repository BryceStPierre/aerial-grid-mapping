import Point from "./Point";
import Line from "./Line";
import GridUnit from "./GridUnit";

/** Class representing a grid on the Cartesian plane. */
export default class Grid {
  /**
   * @param {Rectangle} graph 
   * @param {Line} line 
   * @param {number} size 
   */
  constructor(graph, line, size) {

  }
}